/**
 * @comptext/core/benchmarks — Token benchmark data
 *
 * Pre-measured token counts for all 5 clinical scenarios,
 * benchmarked with tiktoken cl100k_base (GPT-4) and Gemini SentencePiece.
 */

export type { TokenBenchmark } from "./data.js"
export { TOKEN_BENCHMARKS } from "./data.js"
